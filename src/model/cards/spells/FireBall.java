package model.cards.spells;

import javafx.scene.image.Image;

public class FireBall extends Spell {
    public FireBall(Image image, int cost, int radius) {
        super(image, cost, radius);
    }
}
